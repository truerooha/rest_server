# Railway build configuration
# Указываем версию Node.js 20+ для совместимости с better-sqlite3@12.6.2

[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
